"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AmplifyBackend = void 0;
const aws_1 = __importDefault(require("./aws"));
const configuration_manager_1 = require("../configuration-manager");
class AmplifyBackend {
    constructor(context, creds, options = {}) {
        this.context = context;
        this.amplifyBackend = new aws_1.default.AmplifyBackend({ ...creds, ...options });
    }
    static async getInstance(context, options = {}) {
        if (!AmplifyBackend.instance) {
            let cred = {};
            try {
                cred = await configuration_manager_1.loadConfiguration(context);
            }
            catch (e) {
            }
            AmplifyBackend.instance = new AmplifyBackend(context, cred, options);
        }
        return AmplifyBackend.instance;
    }
}
exports.AmplifyBackend = AmplifyBackend;
//# sourceMappingURL=aws-amplify-backend.js.map