"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = exports.name = void 0;
const amplify_prompts_1 = require("amplify-prompts");
const path = __importStar(require("path"));
const constants_1 = require("../constants");
var constants_2 = require("../constants");
Object.defineProperty(exports, "name", { enumerable: true, get: function () { return constants_2.categoryName; } });
async function run(context) {
    if (/^win/.test(process.platform)) {
        try {
            const { run } = await Promise.resolve().then(() => __importStar(require(path.join('.', constants_1.categoryName, context.parameters.first))));
            return run(context);
        }
        catch (e) {
            amplify_prompts_1.printer.error('Command not found');
        }
    }
    const header = `amplify ${constants_1.categoryName} <subcommands>`;
    const commands = [
        {
            name: 'add',
            description: `Takes you through steps in the CLI to add a ${constants_1.categoryName} resource to your local backend`,
        },
        {
            name: 'import',
            description: `Takes you through a CLI flow to import an existing ${constants_1.categoryName} resource to your local backend`,
        },
        {
            name: 'update',
            description: `Takes you through steps in the CLI to update an ${constants_1.categoryName} resource`,
        },
        {
            name: 'push',
            description: `Provisions only ${constants_1.categoryName} cloud resources with the latest local developments`,
        },
        {
            name: 'remove',
            description: `Removes ${constants_1.categoryName} resource from your local backend. The resource is removed from the cloud on the next push command.`,
        },
    ];
    context.amplify.showHelp(header, commands);
    amplify_prompts_1.printer.info('');
}
exports.run = run;
//# sourceMappingURL=storage.js.map