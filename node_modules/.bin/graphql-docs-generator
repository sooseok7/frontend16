#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@aws-amplify/graphql-docs-generator/bin/cli" "$@"
  ret=$?
else 
  node  "$basedir/../@aws-amplify/graphql-docs-generator/bin/cli" "$@"
  ret=$?
fi
exit $ret
