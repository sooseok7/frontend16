/**
 * (experimental) Defines a Tag Option, which are similar to tags but have multiple values per key.
 *
 * @experimental
 */
export declare class TagOptions {
    /**
     * (experimental) List of CfnTagOption.
     *
     * @experimental
     */
    readonly tagOptionsMap: {
        [key: string]: string[];
    };
    /**
     * @experimental
     */
    constructor(tagOptionsMap: {
        [key: string]: string[];
    });
}
